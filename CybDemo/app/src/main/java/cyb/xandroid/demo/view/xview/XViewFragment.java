package cyb.xandroid.demo.view.xview;

import android.os.Bundle;
import android.support.v4.app.Fragment;

import cyb.xandroid.demo.base.BaseActivityListFragment;

/**
 * A simple {@link Fragment} subclass.
 * Activities that contain this fragment must implement the
 * {@link XViewFragment.OnFragmentInteractionListener} interface
 * to handle interaction events.
 * Use the {@link XViewFragment#newInstance} factory method to
 * create an instance of this fragment.
 */
public class XViewFragment extends BaseActivityListFragment {
    @Override
    public void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
    }
}
